# 빌드 컨텍스트에서 뺄 것. 도커 설정이지만 컨텍스트 루트에 있어야 인식되므로
# docker/ 로 옮기지 않고 레포 루트에 둔다.
.git
.github
.gradle
.kotlin
build/*
.idea
.vscode
.fleet
.codex
.claude
docs
*.md
.env
.env.*
!.env.example
**/*.log
.DS_Store

# 배포 스크립트는 이미지에 들어갈 필요가 없다(EC2 가 S3 에서 받는다)
deploy
# 위에서 build 를 통째로 뺐지만, docker/Dockerfile.runtime 은 bootJar 결과물이 필요하다
!build/libs
